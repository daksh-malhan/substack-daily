# PROGRESS — Substack Surprise Magazine

> **Resume file.** If a session ends (credits run out, crash, etc.), a new session reads THIS file first to know exactly where to continue. Keep it current: update the status table + "Current state" after every mini-goal, and ideally after each meaningful step within one.

## How to resume (read this first)
1. Read `PLAN.md` (the spec — decisions + mini-goals MG0–MG12 with success criteria).
2. Read `PLAN-REVIEW-LOG.md` (why things are the way they are — the Codex review history).
3. Read the status table below → find the first mini-goal not `✅ done`.
4. Read **Current state** + **Next action** below for the exact resume point.
5. Continue the kickoff process for that mini-goal: build → self-verify with `/goal` against its success criteria → get Codex sign-off (`codex exec resume … -c sandbox_mode="read-only" --skip-git-repo-check`, scoped to ONLY this mini-goal's criteria) → mark done here → next.
6. **Every `codex exec` in this repo needs `--skip-git-repo-check`** (not a git dir). Codex review thread id is recorded below if a session is mid-review.

## Status
Phase: **1 — Build COMPLETE.** GATE 1 approved. **MG0–MG14 all done (Codex-approved).** Project feature-complete & verified, incl. the MG13 deep-dive brainfood pipeline + the MG14 one-deep-dive-per-press session model. `bun run dev` → ship.

| MG | Title | Status |
|----|-------|--------|
| — | Phase 0: PLAN.md + Codex review (4 rounds, APPROVED) | ✅ done |
| — | GATE 1: human approval of PLAN.md | ✅ approved |
| MG0 | Environment, Codex-contract probe & scaffolding | ✅ done (Codex APPROVED) |
| MG1 | Skeleton, request loop & pretext spike (+ local-server hardening) | ✅ done (Codex APPROVED) |
| MG2 | Newsletter pool & random pick (+ refresh-pool) | ✅ done (Codex APPROVED) |
| MG3 | Content fetcher (RSS + archive, canonicalization) | ✅ done (Codex APPROVED) |
| MG4 | Codex synthesis (budget, hardening, anti-fabrication) | ✅ done (Codex APPROVED) |
| MG5 | Image acquisition (metadata/URL filtering) | ✅ done (Codex APPROVED) |
| MG6 | Persistence / Obsidian vault writer (atomic, hardened DL) | ✅ done (Codex APPROVED) |
| MG7 | Assembly endpoint (concurrency guard, observability) | ✅ done (Codex APPROVED) |
| MG8 | Frontend chrome + vibe presets | ✅ done (Codex APPROVED) |
| MG9 | pretext article-body layout (DYNAMIC: drag image, live wrap, full-width) | ✅ done (Codex APPROVED) |
| MG10 | Offline "Library" mode (network guard, safe asset route) | ✅ done (Codex APPROVED) |
| MG11 | Loading/error UX + real end-to-end (SSE per-stage progress) | ✅ done (Codex APPROVED) |
| MG12 | Full verification | ✅ done (Codex APPROVED) |
| MG13 | Deep-dive brainfood pipeline (archive scrape → article-level curate → cluster → multi-magazine) | ✅ done (Codex APPROVED) |
| MG14 | One deep-dive per press (session cache + 70/30 fresh-vs-reuse) | ✅ done (Codex APPROVED — plan + impl) |

Legend: ⬜ not started · 🟨 in progress · ✅ done · ⛔ blocked


## Current state
**MG0 + MG1 complete and Codex-APPROVED.** Project runs end-to-end at the skeleton level.
- MG1 delivered: Bun server (`src/server/`, loopback-only) serving the Vite-built frontend (`src/web/` → `dist/web`) on one origin; `POST /surprise` returns a stub `Magazine` (shared type in `src/shared/magazine.ts`, imported by both sides); "Surprise me" button renders it; pretext spike flows prose around a floated image (verified 0 overlap at 1200px + 640px via headless browser, reflows on resize); security middleware = loopback Host required, exact same-origin JSON for mutations, cross-origin `/library-assets` reads blocked.
- Checks: `bun run typecheck` clean, `bun run lint` clean, `bun test` **54/54 pass** (text, paths, security, app). `bun run dev` = build web + serve.
- Codex: one MG1 REVISE (exact-same-origin + asset Origin guard) was raised and fixed, then APPROVED.

## MG2 recap
Done + Codex-APPROVED. `config/pool.json` (40 domains), `src/shared/domains.ts` (canonicalize + substack-host test), `src/server/fsutil.ts` (`atomicWriteFile` + `Mutex`), `src/server/pool.ts` (`loadPool`, pure `pickFromPool`, `LastPickStore` locked pick, `parseDiscoverDomains`, mutex-guarded `refreshPool`), `scripts/refresh-pool.ts`, `fixtures/discover.html`. 73/73 tests. One Codex REVISE (mutex around refreshPool read-merge-write) fixed. Live discover adds 0 (client-rendered/custom-domain) — best-effort, fixture-tested. Runtime last-pick state path will live under `.state/` (gitignored); wired in MG7.

## MG3 recap
Done + Codex-APPROVED (one REVISE: resolve relative item links + return resolved domain after redirect — fixed). Files: `src/shared/post.ts` (Post/FetchResult; only `contentText` ever goes to Codex), `src/server/http.ts` (`fetchWithRedirects` + timeout/retry + `FetchError`), `src/server/feed-parse.ts` (`parseRssFeed` + `isSubstackFeed`), `src/server/archive-parse.ts`, `src/server/fetcher.ts` (`fetchPublication`), fixtures (feed.xml 12 items incl 1 paywalled, feed-nonsubstack.xml, archive.html, archive-empty.html). 99/99 tests. Live: theintrinsicperspective.com → 22 posts, redirect followed, images, archive merged; astralcodexten.com → clean 404 FetchError.

## MG4 recap
Done + Codex-APPROVED (one REVISE: bound stderr/stdout, don't await pipe EOF, strict parseSpec — all fixed). `src/server/synth.ts`: `budgetPosts`, `buildPrompt` (UNTRUSTED_SOURCE wrapping + data-not-instructions), `makeCodexRunner` (stdout:ignore, capped+cancellable stderr, SIGKILL timeout gated on proc.exited), `MagazineSpecSchema` (zod) + strict `parseSpec`, `validateAndAttach` (substring anti-fabrication + provenance), `synthesize` (retry-only-on-parse). 118/118 tests incl. fake-codex success + hard-kill-timeout. **Live proof: theintrinsicperspective.com → real codex 36s → 5 sections, 15 excerpts, all 15 substring-verified, 0 fabricated, vibe=vintage-science.** SynthesisResult is the MG5/MG6 input. NOTE: avoid backticks in `codex exec "..."` shell prompts (bash command-substitution).

## MG5 recap
Done + Codex-APPROVED (one REVISE: dedupe test needed a real duplicate URL — fixed). `src/server/images.ts`: `urlLooksLikeIcon`, `substackWidthFromUrl`, `harvestPostImages` (deduped, keyed by postId), `cseImageSearch` (best-effort, returns [] on failure), `acquireImages` → `ImagePlan` (each section gets `{kind:'image',candidate}` or `{kind:'typographic'}`; post-image→vibe→typographic; degrades + warns with no key). **METADATA ONLY — no bytes fetched (bytes are MG6).** `ImageCandidate` = {url, source:'post'|'cse', width?, height?, alt}. Fixtures: `fixtures/cse-response.json`. Manual `bun run smoke:cse`. 127/127 tests.

## MG6 recap
Done + Codex-APPROVED (took 5 review rounds — security-critical). `src/server/image-download.ts`: hardened `downloadImage` (SSRF guard incl. full IPv6 fe00::/8 + mapped-hex + bracketed hosts; byte/time/redirect caps; MIME sniff png/jpeg/webp/gif, reject SVG/HTML; `imageSize` for all 4 formats, REJECT undeterminable dims; sha256 hash). `src/server/vault.ts`: `persistMagazine` (assemble in `library/.tmp/<uuid>/` → atomic `rename` to `library/<slug>/`; `manifest.json` status=complete; content-hash dedupe; per-magazine 24MB byte cap; graceful typographic fallback on image failure), `renderIndexMd` (frontmatter + `![[images/..]]` + blockquoted excerpts w/ source links + `[[themes/<slug>|Name]]`/`[[newsletters/<slug>|Name]]`), `reconcileNotes`+`pruneStaleNotes` (notes = pure function of magazines, crash-self-healing). DNS-rebinding IP-pin + symlink-realpath rejected as out-of-scope for a user-owned local vault (documented in code). 155/155 tests. **Live: persisted The Intrinsic Perspective — 5 sections, 5 real images downloaded, valid vault tree.**

## MG7 recap
Done + Codex-APPROVED (3 review rounds). `src/server/surprise.ts`: `runSurprise(deps, debugId?)` chains pick→fetch→synth→images→persist; injectable `SurpriseDeps` (each stage a closure) so it's fully offline-testable; `makeSurpriseDeps(cfg)` wires the real MG2–MG6 stages from a `SurpriseConfig` (poolPath/statePath/vaultRoot/keys/fetchImpl/runner); `timed()` emits one structured log per stage (ms + counts) under a shared `debugId`; `StageError(stage, cause)` carries the `debugId`; `InFlightGuard` (single-slot, tryAcquire/release). `src/server/app.ts`: `/surprise` now runs the pipeline behind the MG1 security gate — mints the `debugId` BEFORE `guard.tryAcquire()` (so even a busy-409 is traceable), `runSurprise(deps, debugId)`, `finally` release, and `surpriseErrorResponse` returns a sanitized envelope `{error:{debugId,stage,code,message}}` with FIXED public per-stage messages (`STAGE_MESSAGE`) — detail stays in logs only. Status: 409 busy, 502 fetch/synthesize (upstream), 500 pick/images/persist. `createDefaultContext()` reads `GOOGLE_CSE_KEY/ID` from env; `src/server/index.ts` loads `dotenv` and wraps `fetch:(req)=>handleRequest(req,ctx)` (Bun passes the Server as 2nd arg — must not leak into ctx). Codex REVISEs fixed: (1) sanitize envelope messages, (2) correlate image warnings with debugId (threaded via the images stage `warn`), (3) all-5-stage failure tests + guard-release-after-failure + full Magazine runtime-shape assertion, (4) debugId minted pre-guard so busy/internal envelopes carry it. **169/169 tests** (offline e2e through the REAL wired stages on `fixtures/feed.xml` + a fake runner → schema-valid Magazine + real vault entry written + last-pick state; concurrency; per-stage envelopes; real-socket loopback + forged-Host).

## MG8 recap
Done + Codex-APPROVED (1 REVISE: image-src validation bypass — fixed). `src/web/vibe.ts` (pure): `VIBE_PRESET_IDS` (mirrors `synth.ts` VIBE_PRESETS), `resolveVibePresetId` (unknown→`classic-editorial`), `vibeClass`, `safeAccent` (re-validates `#rrggbb` before it touches a style attr — defense-in-depth CSS-injection guard). `src/web/render.ts` (pure, no DOM, fully unit-tested): `escapeHtml`, `safeHttpUrl` (http/https else `#`), `safeLocalImageSrc` (**strict allowlist** `^[A-Za-z0-9][A-Za-z0-9._/-]*$` + reject `//` and `..` segments — resists browser-normalization bypasses: leading/trailing whitespace, embedded tab/newline, protocol-relative, percent-encoded traversal), `magazineHostAttrs`, `renderMagazineHTML` (masthead/kicker/title/byline/theme-pills + drop-cap intro + sections with dividers/figures/captions/prose + cited blockquote excerpts + footer; every interpolated value escaped; source links `target=_blank rel="noopener noreferrer"`). `src/web/main.ts` = DOM wiring only (fetch `/surprise` → set class + `--accent` + innerHTML; `img.onerror` drops broken figures; basic status text). `src/web/index.html` = app-bar + nav (Surprise me works; Library disabled until MG10) + `#magazine`. `src/web/styles.css` = base chrome + **6 vibe-* themes** via CSS vars (classic-editorial, modern-minimal, vintage-science, retro-tech, zine, mono-serif), **system/web-safe fonts only — no remote @import/CDN** (offline groundwork for MG10). **Verified visually**: headless screenshot of all 6 presets confirmed each is distinct in type + color. `src/web/render.test.ts` (12 tests). 181/181 tests; web build OK (`bun run build:web`).

## MG9 recap (DYNAMIC layout — user-amended)
Done + Codex-APPROVED (3 REVISE rounds). **User amended the spec mid-MG9** (see PLAN.md MG9): they wanted the [pretext dynamic-layout](https://chenglou.me/pretext/dynamic-layout/) effect — **draggable image + LIVE text reflow + full page width** — not just resize-reflow. Rebuilt accordingly.
`src/web/layout.ts` (pure, DOM-free, unit-tested): `debounce` (trailing+cancel) + `REFLOW_DEBOUNCE_MS=150`; `rafThrottle(schedule,cancel,fn)` (injectable rAF coalescer for live drag); `imageSizeFor(col)` (~40% col, clamped [120,360], **never > 80% of a narrow column**); `rectsOverlap`; `clampObstacle` (keep image in column); `FlowEngine<C>` (injectable breaker); **`availableSegments(top,p)`** (left/right gaps around the obstacle on a line band, each ≥ `MIN_SEGMENT_WIDTH=48`, edge stops `gap` short of the image → no overlap); **`flowAround`** (steps down filling each band's segments L→R; fully-blocked bands produce no lines and it steps past; no-progress + `maxLines` guards); `flowHeight`.
`src/web/article-layout.ts` (DOM driver): `layoutArticle` swaps each `.mag-section` figure+prose for a `.mag-flow` host with a **persistent draggable `<img>`**, measures real computed font (`"normal"` fallback), runs pretext via `flowAround`, positions image+lines. **Drag**: pointerdown on image → move/up on **window** (survives leaving the image) + `lostpointercapture` end + `setPointerCapture` try/catch + **active-pointerId guard** (multi-touch safe); live reflow is **rAF-throttled**, frame cancelled before the final settle; client→column coord conversion via host rect. **Resize**: debounced ResizeObserver, **width-only guard** (own height changes can't loop it), re-clamps the dragged obstacle. Disposer cancels frames/timers + detaches ALL listeners (incl. the named image-`error` handler → a late failure can't touch a retired layout). Image-less/broken-image sections render plain prose.
`src/web/styles.css`: **full-width `.magazine`** (`max-width:none`); `.mag-flow`/`.mag-flow-img` (JS left/top, `cursor:grab`/`grabbing`, `touch-action:none`, hover/drag outline)/`.mag-flow-line` (+ `.mag-flow > .mag-prose` fallback). `layout.test.ts` = 23 tests (measured 100–250ms settle; rafThrottle coalesce/cancel; imageSizeFor caps at 320/200/140/90/50; availableSegments; flowAround no-overlap across 5 obstacle placements + both-sides wrap + full-width above/below + termination + cap). **Headless-verified**: full-width (col 1101px @1280); image top-right → 8 left lines 0 overlaps; **synthetic drag to center → LIVE reflow to 8 left + 8 right lines wrapping both sides, 0 overlaps, each row reads continuously L→R across the image**. 204/204 tests; web build OK.

## MG10 recap (Offline Library — security-heavy)
Done + Codex-APPROVED (3 REVISE rounds). **`src/server/library.ts`** (the file-serving/delete trust boundary): `validateEntryId` (id must round-trip through `slugify` unchanged AND not be a reserved dir — rejects `..`/`/`/dotfiles/uppercase/space/unicode); `listLibrary` (complete-manifest only, newest-first, skips dotfiles/`themes`/`newsletters`/incomplete); `readMagazine` (validated id + complete + **zod `MagazineSchema` shape validation** + **`magazine.id === entryId`** so a corrupt/imposter entry can't poison or impersonate); `resolveAsset` (serves ONLY `images/<file>` of a complete entry — exactly 2 segments, `segments[0]==="images"`, no dotfiles, known image ext, **double `resolveInVault`** [vault→entry, entry→images], `stat` regular file); `deleteEntry` (validated id + complete check + `rm` + `reconcileNotes`). **`src/server/app.ts`** routes: `GET /api/library`, `GET /api/library/:id`, `DELETE /api/library/:id`, `GET /library-assets/:id/*`; `classifyRoute` maps library JSON reads + assets to **"asset"** kind (cross-origin blocked) and DELETE to **"mutating"**; handlers `decodeURIComponent` each segment in try/catch (→400) so `..%2f` is decoded then rejected; **library/asset handlers wrapped in try/catch → clean JSON 500** (no stack/path leak); `VAULT_ROOT` env override added. **`src/server/security.ts` hardened** (Codex): `originMatchesHost` now compares **full origin** (scheme+host+port, rejects cross-scheme https); `isJsonContentType` parses media type **exactly** (rejects `text/plain; x=application/json`); **`Sec-Fetch-Site` defense** on asset+mutating (rejects cross-site `<img>` that omits Origin; absent/`none` allowed for curl/navigation). **`src/web/render.ts`**: `assetUrl(id, src)` builds same-origin `/library-assets/:id/<rel>` (validates slug id + `safeLocalImageSrc`) — images now load online AND offline; `renderLibraryList` (escaped, `data-id` open/delete). **`src/web/main.ts`**: Library button → `/api/library` → list; click-delegation open/delete (same-origin fetches only). index.html (Library button enabled + `#library-list`) + styles.css (`.lib-*`). **Verified**: 223/223 unit tests incl. `library.test.ts` (real vault via `persistMagazine`; traversal/dotfile/non-image/incomplete/bad-id/cross-origin/Sec-Fetch-Site/malformed-json/imposter/IO-error-500 attacks); **live offline curl** (real server on a seeded temp vault): list newest-first, magazine read, asset PNG 200, `..%2f` traversal/`manifest.json`/bad-id → 404, cross-origin → 403, only remote URL is the substack source link. web build OK. (Note: a shared/contaminated playwright browser was firing real `/surprise` POSTs during testing — confirmed NOT my code: built JS has no auto-call/timers; offline verification done via curl instead.)

## MG11 recap (Loading/error UX + real e2e — SSE progress)
Done + Codex-APPROVED (2 REVISE rounds). **`POST /surprise` streams Server-Sent Events** when `Accept: text/event-stream`, reusing the MG7 per-stage `debugId` logs for REAL progress (plain-JSON path kept for tests). `src/server/app.ts`: `streamSurprise` builds a ReadableStream that tees completed-stage logs into `event: stage` frames, ends with `event: result` (Magazine) or `event: error` (shared sanitized `surpriseErrorInfo`, fetch/synth→502 else 500), sends a **5s `: keepalive` heartbeat**, and on a **client `cancel()`** stops the heartbeat + silences a guarded `send()` (a late enqueue no-ops, never throws→aborts the pipeline). **Guard released ONLY in the pipeline's finally** (even after disconnect) so an abandoned build can't race a new one; codex hard-timeout bounds it. Stage filter requires `typeof entry.ms === "number"` so image WARN logs (stage:images, no ms) don't falsely advance the UI. **Per-request timeout**: `handleSurprise` calls `server.timeout(req, 250)` for `/surprise` only (Bun's `server` threaded through `handleRequest`); the server-wide `idleTimeout` was reverted (fixed the bug where Bun's default 10s killed the SSE stream mid-synthesis). `src/web/progress.ts` (pure, unit-tested): `INITIAL_STATUS`, `statusForStage` (completed-stage→forward-looking line), `parseSse` (buffer→events+rest, multi-line data, partial-frame carry). `src/web/main.ts`: `surprise()` disables button + streams via `consumeStream` (status per stage, render on result, `showError`+Retry on error / 409 / premature-EOF); `#retry` button wired; button re-enabled in finally. index.html/styles.css: status area + hidden Retry. **231/231 tests** (progress.test + app SSE: streams stages→result, error event no-leak + guard freed, warn-doesn't-advance, disconnect-frees-guard). **Real e2e proven** (network on, isolated temp vault): full pick→fetch→synthesize→images→persist→result, 10 heartbeats through the ~50s codex stage, persisted a COMPLETE magazine (*Crypto Trader Digest*, 5 sections / 15 excerpts / 5 images, retro-tech); unfetchable random picks emit clean `event: error` (the Retry path).

## MG13 — Deep-dive brainfood pipeline (✅ DONE — Codex APPROVED, user-requested)
**Goal (user spec):** bias strongly toward conceptually deep "brainfood" (philosophy, psychology, culture, meaning, human behavior, creativity, literature, spirituality, reflective essays) — NOT AI/tech/startup/news. Discover brainfood Substacks → pull their **archive** → scrape **full** article content → **evaluate & filter at the ARTICLE level** (discard weak/shallow/tech-AI/irrelevant, WITHOUT eliminating the whole Substack) → **cluster** related deep articles into themes → write a **deep-dive magazine per strong cluster**, citing/linking EVERY article used. A magazine may span multiple articles or be one especially strong article.
**Decisions (user):** per Surprise produce **every strong cluster** (multiple magazines/run); scan **~50 archive articles**/run.
**Verified data approach (live):** Substack `GET https://<canonical-domain>/api/v1/archive?sort=new&limit=12&offset=N` (paginate by 12) → JSON list with `canonical_url`, `title`, `audience` (filter `=== "everyone"` for FREE = full content, skip paid teasers), `wordcount` (cheap depth pre-filter), `post_date`, `slug`. Archive listing does NOT carry full body (`body_html` empty, `truncated_body_text` ~64 chars) — fetch each free post's `canonical_url` page and extract the article body (`<article>` / `div.available-content`) → toPlainText. RSS already gives full content for the recent ~20 (reuse where possible). NOTE: some pubs moved off *.substack.com (e.g. gurwinder → gurwinder.blog) — the fetcher follows redirects; pool updated.
**Pool (LIVE-VERIFIED 2026-06-21):** every domain in `config/pool.json` was probed through the REAL `fetchPublication` — **29 brainfood Substacks, all confirmed to serve a valid Substack feed with real posts.** This fixed the runtime failures the user hit (logs 2026-06-20): the old pool had domains that fail `isSubstackFeed`/return 404/400/521 (`adamtooze.com`, `astralcodexten.com`, `thebignewsletter.com`, `profgalloway.com`, `thediff.co`, `thefp.com`, `public.news`, `theankler.com`, `garbageday.email`, `gurwinder.blog`, `honest-broker.com`, `roxanegay.substack.com`, `brianklaas.substack.com`, `bookbearexpress.substack.com`). Rule going forward: **prefer the `<handle>.substack.com` form** (reliably serves `/feed`; the fetcher follows the redirect to any custom domain) and verify before adding. Replacements found live: astralcodexten.com→astralcodexten.substack.com, gurwinder.blog→gurwinder.substack.com, honest-broker.com→tedgioia.substack.com, roxanegay→theaudacity.substack.com, brianklaas→forkingpaths.substack.com, bookbearexpress→ava.substack.com; dropped adamtooze/garbageday (economics-news/internet-news, off-brief + off Substack); added 7 verified deep pubs (commonreader, drmaciver/Overthinking Everything, personalcanon, summerbrennan, thefitzwilliam, thomasjbevan, uncertaintymindset). NOTE: the user's 2026-06-20 logs were the OLD single-magazine `runSurprise` build (no `curate` stage, 120s codex timeout) — a fresh `bun run dev` now runs the deep multi-magazine pipeline with the 240s timeout + this verified pool.
**Pool (superseded):** rebalanced `config/pool.json` to **24 curated brainfood Substacks** (removed tech/AI/finance/news: slowboring, notboring, platformer, lennysnewsletter, thediff, thezvi, garymarcus, taibbi, kyla, natesilver, etc.; added escapingflatland, gurwinder, robkhenderson, sashachapin, philosophybear, conspicuouscognition, theconvivialsociety, wisdomofcrowds, countercraft, roxanegay, whyisthisinteresting, simonsarris, brianklaas, nonzero, bookbearexpress, kneelingbus). `pool.test` size floor lowered 30→18 (curated quality over breadth; auto-discovery deepens it). The brainfood bias is ALSO enforced at the article level so a mixed pub still contributes only its deep pieces.
**STATUS: P1–P5 ALL BUILT + live-verified end-to-end + Codex APPROVED (5 review rounds).** `POST /surprise` now runs the deep pipeline (multi-magazine). **255 tests green; typecheck + lint clean.** **Live proof** (experimental-history.com, real codex): deep-fetch → curate produced **7 strong brainfood clusters** with substantive themes (e.g. "How commercialization domesticates rebellion and artistic independence", "How failed ideas persist in scientific knowledge production", "Social awkwardness as a self-reinforcing error") → per-cluster synth produced a deep-dive (3 sections, 9 excerpts, **citing both source articles 2/2**, 115s). FIX applied: codex runner default timeout 120s→**240s** (deeper prompts are slower; SSE heartbeat keeps the stream alive) — this was likely one of the runtime errors the user hit; per-cluster prompt asks 3-5 sections (not 5-7) since a cluster is 1-3 articles; `MAX_MAGAZINES=6` + `MAX_RUN_MS=9min` runaway/time caps.

**Codex sign-off (scoped to MG13), final-arbiter resolutions across 5 rounds:**
- R1: enforce citation prompt; SSRF-guard archive `canonical_url`/redirects (`assertSafeUrl`/`hostIsPublic` + same-publication); curate `MAX_IN_PROMPT` 40→60 + log; bound lifetime (`MAX_RUN_MS`).
- R2: total fetch budget (`FETCH_BUDGET_MS`); article redirects confined to own domain (`expectedDomain`); body byte cap (`readCappedText`). (citation-as-excerpt + SSE-not-JSON accepted by Codex.)
- R3: `safeSlugBase` (long/emoji cluster title → bounded ≤80-char safe slug, falls back, never throws — one weird theme can't abort a partial multi-mag run); capped archive JSON read; CSE 10s `AbortController`.
- R4→R5 (**APPROVED**): **#1** empty archive-ref STUBS (`contentText:""`) no longer shadow the full archive-API body — merge extracted to pure, unit-tested `mergeDeepArticles` which drops body-less posts; **#2** the deep synth now passes `budget:{maxArchive,maxPosts = cluster.articles.length}` so EVERY cluster article reaches the prompt (the default `maxArchive=8` was silently dropping a cluster's 9th+ article since they're all `source:"archive"`); **#5** `readCappedText(res, maxBytes, timeoutMs)` races each read against the deadline, checks the cap BEFORE appending (strict), and CANCELS the reader in `finally`. **#3** (MAX_RUN_MS gates cluster starts; each cluster independently hard-bounded → total ≤ MAX_RUN_MS + one-cluster-max, guard freed once in `finally`) and **#4** (frontend always uses heartbeat'd SSE; JSON path frees the guard exactly once even on disconnect) accepted with rationale. New tests: stub-shadow guard, 12-article cluster admits all 12.
**Phased plan (DONE):**
- **P1 `archive-api.ts` — ✅ DONE + live-verified.** `listArchive` (paginated archive API, skips non-newsletter types), `isFreeArticle`, `fetchArticle` (post page → `.available-content .body.markup` → inert `Post.contentText` + images), `fetchDeepArticles` (free + `wordcount>=400`, fetch full content). 4 offline tests (`archive-api.test.ts`). **Live**: experimental-history.com → 12 listed → 9 deep articles with FULL content (20k/16k/4k chars) + images. 240 tests total.
- **P2+P3 `curate.ts` — ✅ DONE.** ONE codex pass evaluates + clusters: discards shallow/tech/AI/news, clusters deep brainfood survivors into themes, marks strength; SHORT opaque ids (a0…) so codex never sees URLs; zod-validated; `strongClusters` filter. 4 tests. Anti-fab posture preserved (inert excerpts, UNTRUSTED_SOURCE).
- **P4 per-cluster synth — ✅ DONE.** `synthesize` gained `theme`/`title` opts (theme-focused prompt, distinct title); `SynthesisResult.title` threaded through `images.ts` → `vault.ts` (Magazine.title + slug derive from the title so multiple deep-dives from one pub get distinct folders; `newsletter` still drives the [[newsletter]] graph link). Substring anti-fab validates vs the ORIGINAL article text. Backward-compatible (title defaults to publication).
- **P5 multi-magazine output — ✅ DONE.** `deep.ts` `runDeepDive` + `makeDeepDeps` (injectable, offline-tested, 4 tests): pick → `deepFetch` (RSS full posts + ~50 archive articles, free only, dedup) → curate → per STRONG cluster: synth(theme)→images→persist, emitting each via `onMagazine`. `app.ts` now uses runDeepDive: SSE streams a `result` per deep-dive + a final `done {publication, count}`; JSON returns `{publication, magazines[]}`. `surprise.ts` Stage gained "curate". **Frontend** (`main.ts`/`progress.ts`/index.html/css): docket shows deep PHASES (pick/fetch/curate/writing) + a live "pressed N" line; on `done` routes — 0→retry msg, 1→read it, N→a `#results` set of openable cards. Tests updated (app.test deep multi-magazine, progress.test deep phases).

## Out-of-band changes since MG11 (not mini-goals)
- **Light/Dark theme toggle.** Added a warm **light mode** (beige press desk `--desk #e6dac1`, espresso-brown ink, kraft-rust accent `--flag #bd5430`) alongside the dark "Press" theme, via `:root[data-theme="light"]` + an `--on-desk`/`--hair` token flip. Header **#theme-toggle** button; theme set pre-paint by an inline `<head>` script (no FOUC), persisted to `localStorage("press-theme")`, defaults to OS `prefers-color-scheme`. Both verified by headless screenshot.
- **Substack depth — DONE.** Root cause of "surface level" was the synthesis budget, not the pool. `synth.ts` `budgetPosts` defaults deepened: `maxPosts` 18→**22**, `maxCharsPerPost` 2000→**4000**, `maxArchive` 4→**8**; `buildPrompt` now demands SUBSTANTIVE connective tissue (name real ideas/people/arguments), **5–7 sections**, **2–3 verbatim excerpts each**. **Verified live**: Lenny's Newsletter → **6 sections / 18 excerpts / 4.6k prose chars** (was ~5/15), synth 81s (within the 255s timeout + heartbeat). **Pool decision:** the 40-domain `config/pool.json` is already a hand-curated set of top Substacks (≈ a manual Discover); live `substack.com/discover` is JS-rendered (static scrape yields ~0), so NO fragile scraping was added and NO unverified domains were padded in (they'd just raise the fetch-fail/retry rate). `refresh-pool` stays the best-effort grow path. Login-scraping (paywalled posts) remains an unbuilt, out-of-scope optional layer.
- **Frontend redesigned — "The Press" identity** (user said the old cream/serif/terracotta look was "too bad"; it was literally AI-default #1). New shell: blueprint-navy press desk, warm proof-paper bed framed by **registration crosshairs**, **risograph two-tone ink** (`--flag` fluoro pink #ff4d6d + federal blue ink), **mono "press voice"** chrome, offset-shadow riso buttons. Five views on the bed (idle hero / **live build docket** / error / magazine / back-issues), managed by `showView` in `main.ts`. The **docket stamps each real SSE stage** (`progress.ts` `PRESS_STEPS`/`docketStates` — pure, tested): Choosing→Pulling→Setting type→Inking→Binding. Library = "Back issues" press-ticket cards. Touched `index.html`, `styles.css` (chrome rewritten; **magazine-body + 6 vibe presets + `.mag-flow*` drag layout kept intact**), `main.ts`, `progress.ts` (+tests). render.ts markup UNCHANGED (tests green). 234/234 tests; verified via isolated headless design-board screenshot. **CLAUDE.md updated** to current architecture.
- **Substack source — decided, NOT yet built (backlog):** user picked "pull fresh from substack.com Discover" + flagged content feels "surface level". Plan (no login needed): (1) make the pool source primarily from `substack.com/discover` via the existing `refreshPool`/`parseDiscoverDomains` (currently best-effort/manual `bun run refresh-pool`); (2) **deepen synthesis** — raise `budgetPosts` limits in `synth.ts` (`maxPosts` 18, `maxCharsPerPost` 2000 truncation is the main "surface level" cause; public RSS already carries full free-post bodies) and pull more archive. Login-scraping (paywalled posts via the user's session) was discussed as a fragile optional later layer, out of original scope — do NOT build without re-confirming. Everything still verifies feeds via `isSubstackFeed`, so all picks are already real Substack.

## MG12 — Full verification (in progress)
**Green sweep:** `typecheck` clean · `lint` **fully clean (0 warnings)** — fixed the last pre-existing `vault.test` shadow · `bun test` **235/235** · `build:web` ok · `doctor` = bun 1.3.14 + codex 0.141.0.

**Top-level success criteria (PLAN.md) — evidence:**
1. Surprise → random REAL Substack → magazine in the build window — ✅ live: Lenny's Newsletter streamed pick→…→result (81s synth); pool = 40 curated Substacks; `isSubstackFeed` verifies every feed (MG2/MG3).
2. Codex intro + section connective tissue + real excerpts with working links; NO fabricated quotes — ✅ `synth.ts` `validateAndAttach` substring anti-fabrication + server-attached provenance (MG4, `synth.test.ts`); live magazines all-substring-verified.
3. Images present (post + CSE) + pretext text-flow + reflow — ✅ `images.ts`/`image-download.ts` (MG5/6); MG9 DYNAMIC drag-flow headless-verified 0-overlap both-sides + resize settle.
4. Content-chosen vibe preset, cohesive — ✅ 6 `vibe-*` presets, `accentColor` validated (MG8, headless screenshots) + the new light/dark Press shell.
5. Auto-saved to Obsidian vault, local images, `[[theme]]`/`[[newsletter]]` graph links — ✅ atomic `persistMagazine` + `reconcileNotes` (MG6, `vault.test.ts`); live vault entries complete.
6. Offline Library, no net + no codex — ✅ `library.ts` + `/api/library` + `/library-assets` (MG10); live offline curl: list/read/asset-200, traversal-404, cross-origin-403, only-local-assets.
7. No Claude API; codex exec; no API-key billing — ✅ synthesis is `makeCodexRunner` (local `codex exec`) only; invariant enforced repo-wide.
8. typecheck + lint clean; tests pass — ✅ above.

**Online build + offline revisit:** both proven (Lenny's online; MG10 curl offline). **Regression:** the 235-test suite covers every prior MG invariant; all green after the depth + theme + redesign changes.

Remaining: fresh-thread Codex sign-off scoped to MG12 (whole project vs. criteria) → mark Phase 1 COMPLETE.

## Next action (after MG12 sign-off)
Project is feature-complete. Optional follow-ups only: (a) broaden the pool with user-named newsletters; (b) optional login-scraping layer for paywalled posts (out of scope, needs re-confirm). Otherwise: ship — `bun run dev` and enjoy. Read MG12 Do-items + success criteria in PLAN.md. Do: (a) green sweep — `bun run typecheck` + `bun run lint` + `bun test` (full suite) + `bun run build:web`, all clean; (b) walk the **top-level success criteria** (end of PLAN.md) one by one and mark each PASS with concrete evidence (cite the MG/test/live-run that proves it — most are already proven: random real pick, codex connective tissue + substring-verified excerpts + working links, images + pretext drag-flow + reflow, content-chosen vibe, auto-saved Obsidian vault with local images + [[theme]]/[[newsletter]] graph links, offline Library with no net/codex, no Claude API, clean checks); (c) **one online build** (real `/surprise`, network on → rendered magazine + vault entry — already shown for Crypto Trader Digest) **and one offline Library revisit** (network+codex off → render a saved magazine via /api/library + /library-assets — already shown via curl in MG10); (d) a quick **regression re-check** of each prior MG's invariant (no Claude API; inert-text-only to codex; substring anti-fabrication; image-download SSRF/caps; atomic vault; loopback+CSRF+Sec-Fetch-Site; asset-route containment; drag-layout no-overlap; SSE guard lifecycle). Produce a checklist with evidence. Use isolated `VAULT_ROOT=` + curl/controlled checks (shared playwright browser fires stray /surprise POSTs). Then: typecheck+lint+test green → fresh-thread Codex sign-off scoped to MG12 (the whole project against top-level criteria) → mark Phase 1 COMPLETE in PROGRESS → `graphify update .`. After MG12 the project is done — consider a final summary for the user. Read MG11 Do-items + success criteria in PLAN.md. Add: (a) **progress/loading state** during the ~30s–2min build — disable the Surprise button, show **per-stage status messages** (the pipeline already emits structured per-stage logs with a `debugId`; surface stage progress to the UI — simplest is a staged status text / spinner since the POST is a single request, optionally an SSE/stream later but a single-request staged affordance is fine for MG11); (b) **error state with retry** — on a structured error envelope show the message + a Retry button; (c) **"Surprise me again"** affordance after a result. Keep the MG8 chrome / MG9 drag layout / MG10 library intact. Success: during a real build the button is disabled and progress shows; a forced failure shows a retry affordance; a **real** run against a **real** random newsletter produces a rendered magazine + a vault entry (network on). Unit-test the pure UI-state logic (e.g. a small state machine: idle→loading→result|error→retry). Then per-MG workflow: typecheck+lint+test green → headless check (loading + forced-error + a real run) → fresh-thread Codex sign-off scoped to MG11 → update PROGRESS → `graphify update .`. NOTE: there's a shared playwright browser in this env that may fire stray `/surprise` POSTs — use an isolated/temp vault (`VAULT_ROOT=`) and curl/controlled checks; don't trust ambient browser state.

## MG14 plan (one deep-dive per press) — 🟨 planned, Codex review pending
**Goal (user-driven):** one "Surprise me" press = **one** deep-dive magazine, not the whole batch. The next press yields **another** deep dive — a new theme from an already-fetched publication (reusing its articles, no re-fetch) **or** a freshly picked publication — and **never repeats a theme already shown** this session. Agreed selection policy: **70% fresh pick / 30% reuse** a cached pub's un-shown theme; empty fresh picks auto-retry (≤4); same **"Surprise me"** button drives every press.

**Spec:** see `PLAN.md` → **MG14** (full Do/Success). Summary of the change set:
- `deep.ts`: replace the batch `for (const cluster of strong)` loop with **press-one-per-call**; new `DeepCache` (`pubs: Map<resolvedDomain,{newsletter:string,clusters}>` + `pressed: Set<key>`, key = `resolvedDomain::theme`); split into `acquireFreshPub` (prefer-uncached pick → fetch → key by **resolved** domain → curate; ≤4-attempt loop where throw/empty-curate/already-exhausted each spend an attempt, plus a `MAX_PRESS_MS=6min` wall-clock guard), `pressCluster` (random un-pressed cluster → synth→images→persist→mark pressed *after* persist), and `runDeepDive(deps, cache, debugId)` orchestrator (reusable && `random()<0.3` → reuse, else fresh; fresh-`null` with reusable → fall back; neither → `{debugId,publication:"",magazines:[]}`). `DeepResult.magazines` now holds **0 or 1**; `onMagazine` fires exactly once on success, never on the empty/error path. Remove `MAX_MAGAZINES` + batch `MAX_RUN_MS` (add `MAX_PICK_ATTEMPTS=4`, `MAX_PRESS_MS`). Inject `random:()=>number` into `DeepDeps` (default `Math.random`).
- `app.ts`: `SurpriseContext` gains `cache: DeepCache` (empty init); pass into `runDeepDive`. **SSE shape unchanged** — `stage`* → **one** `result` → `done {magazines:1}`, or **zero** `result` → `done {magazines:0}`. Guard/lifecycle unchanged.
- `CLAUDE.md`: update the deep-dive description (no more "N at once"), document the cache model + new bounds + injected `random` + one-result SSE contract. Run `graphify update .` after build.
- Client: **no logic change** — already renders each streamed `result` and tolerates `magazines:0`.

**Codex review log (gate for MG14):**
- **Round 1 → REVISE** (thread `019ee8d2-518f-76a1-9644-dbba9818bff9`). 8 findings, all incorporated into PLAN.md MG14: (1) explicit `DeepResult` shape + exactly-one/zero `onMagazine`; (2) retry attempt classification (throw vs empty-curate vs exhausted-cache all spend an attempt); (3) precise never-repeat invariant = `(resolvedDomain, theme)`, cross-pub theme-string collisions are different content by design; (4) `Newsletter` was a non-existent type → `string`, key cache by **resolved** domain (alias redirects); (5) clarified 70/30 is *branch* probability + prefer-uncached fresh picks; (6) reinstated a wall-clock bound (`MAX_PRESS_MS`) since 4 attempts ≠ a time bound; (7) update CLAUDE.md docs; (8) expanded test list (exhaustion, pool exhaustion, redirect keying, zero/one-result SSE).

- **Round 2 → REVISE** (same thread). 4 follow-ups, all incorporated: (1) redirect reuse was contradictory ("no second fetch" — but the fetch is what reveals the resolved domain) → fixed to "fetch happens; on resolved-domain collision skip re-`curate`/overwrite", test asserts curate-call-count not fetch-count; (2) "prefer uncached" now a concrete mechanism — `pick` gains an `exclude: Set<poolDomain>`, best-effort, with the resolved-domain dedupe authoritative; (3) `MAX_PRESS_MS` clarified as an **acquisition-start** deadline, and `now:()=>number` injected for a deterministic deadline test; (4) real-run invariant re-keyed to `(resolvedDomain, theme)`.
- **Round 3 → REVISE.** 3 precision nits incorporated: (1) `MAX_PRESS_MS` named "acquisition-start deadline" consistently; (2) over-precise overrun formula fixed — `deepFetch` is bounded by HTTP/readCappedText caps (not codex), image byte-downloads by `image-download.ts` caps; (3) exclude-set lifecycle made precise (init from cached resolved domains that are also pool domains; add each selected pool domain before fetch).
- **Round 4 → REVISE.** 2 self-contradictions fixed: (1) dropped the "finite/bounded tail" claim (contradicted untimed `persist`) → now states there is **no hard whole-press wall-clock guarantee**, only per-stage bounds; corrected image-download caps to the persist/download work (`images`/`acquireImages` is metadata-only); (2) pool-exhaustion no longer falls through to an unrestricted re-pick → returns `null`, preserving the never-re-pick-a-spent-domain guarantee.
- **Round 5 → ✅ APPROVED.** Timing semantics + stage bounds accurate; pool exhaustion terminates without re-picks; 4-attempt + deadline guards prevent infinite loops; redirect dedupe / cache identity / SSE cardinality / `(resolvedDomain, theme)` uniqueness coherent; reuse + empty-result fallbacks fully specified.

**Build status:** ✅ DONE. Implemented via TDD. `deep.ts` rewritten (DeepCache/newDeepCache, acquireFreshPub, pressCluster, one-per-press runDeepDive(deps,cache,debugId), injected random/now, removed MAX_MAGAZINES/MAX_RUN_MS, added MAX_PICK_ATTEMPTS=4/MAX_PRESS_MS=6min/REUSE_PROB=0.3); `pool.ts` pickFromPool/LastPickStore.pick gained `exclude` (HARD constraint; lastPick SOFT); `app.ts` SurpriseContext gained `cache`, both runDeepDive call sites updated. Tests = one-per-press contract (deep.test.ts 16 cases; app.test.ts acquisition-failures retry→empty-200, only press stages error + empty-SSE).

**Verified:** `bun test` **266/266**, `bun run typecheck` + `bun run lint` clean. `CLAUDE.md` updated; `graphify update .` (576 nodes). **Real run** (network+codex): press 1 → "Overthinking Everything" (self-knowledge theme), press 2 → "Wisdom of Crowds" (state-legitimacy theme) — each exactly ONE `result` → `done {magazines:1}`, distinct themes, both auto-saved to `./library/`. (Both presses hit the 70% fresh branch; the 30% reuse path is deterministically unit-tested.)

**Codex impl sign-off (fresh thread `019ee8f1-6e02-7ef1-b1a0-7e33d4ecb0d0`):** R1 REVISE — (1) pickFromPool combined exclude+lastPick as equal "avoid" (false exhaustion when sole un-excluded domain IS lastPick) → exclude now HARD, lastPick SOFT; (2) freshly-curated zero-strong logged `exhausted` → now `noStrongClusters`, `exhausted` reserved for fully-pressed pubs; (3) test gaps. R2 REVISE — add explicit cached-exhausted classification test (alias resolving post-fetch to a fully-pressed cached pub). R3 → **✅ APPROVED** (Codex ran the suite itself).

## Key facts to carry forward
- **No Claude API.** Synthesis = local `codex exec -s read-only --skip-git-repo-check --json -o <tmp>`. Privacy boundary: invocation ≠ inference (source text may leave the machine).
- Stack: **Bun + TypeScript**, Vite vanilla-TS frontend, `@chenglou/pretext` for article-body text-flow, CSS chrome.
- `./library/` doubles as the Obsidian vault AND the website's offline data store. Auto-save every magazine. Graph via `[[themes/<slug>|Name]]` + `[[newsletters/<slug>|Name]]` alias links.
- Codex review thread (Phase 0): `019ede8b-f3ba-7553-b363-60439c082421` (started a fresh thread per mini-goal in Phase 1).
- Offline "Library" mode + online "Surprise me" mode in one app; server binds 127.0.0.1 only.

## Environment notes (carry forward)
- **Bun lives at `~/.bun/bin/bun`** and is NOT on the default sandbox PATH. Prefix bun commands with `export PATH="$HOME/.bun/bin:$PATH";` (it was added to `~/.bash_profile`).
- **Codex CLI: 0.141.0** (Homebrew cask). A stuck/very-slow `codex exec` usually means the Codex app is mid-update — run `codex update`, then retry. Every `codex exec` here needs `--skip-git-repo-check` (not a git repo); `< /dev/null` so it never blocks on stdin.
- Network-using commands (installs, `codex exec`, `probe:codex`) need the Bash sandbox disabled.
- **graphify** knowledge graph installed (`uv` at `~/.local/bin`, `graphify` CLI there too; graph in `graphify-out/`, gitignored). Query it (`graphify explain/path "..."`) instead of grepping. **Run `graphify update .` after each mini-goal** to keep it current. CLAUDE.md documents it.
- **CLAUDE.md exists** — high-level architecture + invariants + workflow for future sessions.

## Decisions / changes made mid-session
- Lint = `oxlint` (zero-config, fast). Typecheck = `tsc --noEmit` with `tsconfig.json` `types: ["bun","node"]`.
- RSS/XML parser = `fast-xml-parser`; HTML parser = `node-html-parser`.
- **Build loop = NONE (Ralph loop dropped after MG1, by user decision).** The Ralph loop added friction without real benefit in this interactive, human-steered workflow; the rigor lives in the process below, not the loop wrapper. The active loop + `.claude/ralph-prompts/` were removed. (The global ralph-loop plugin was hardened earlier — `--prompt-file`, `printf` state write, `<promise>` docs — and is left in place for any future hands-off/unattended run; it is just not used here.)

## How to run each mini-goal (no Ralph loop)
For MG2…MG12, in order:
1. Read the mini-goal's Do-items + Success criteria in PLAN.md.
2. Build it; keep `bun run typecheck`, `bun run lint`, `bun test` green (prefix bun with `export PATH="$HOME/.bun/bin:$PATH";`).
3. Self-verify every success criterion with concrete command output (and a real run / headless check where the criterion is behavioral).
4. Fresh-thread Codex sign-off scoped to ONLY that mini-goal:
   `codex exec -s read-only --skip-git-repo-check --json -o /tmp/codex-mgN.txt "<scoped review prompt>" < /dev/null` (sandbox disabled for network). Address any REVISE, re-verify, repeat until APPROVED.
5. Update this PROGRESS.md (mark done, set next) and move on.

## Open questions / blockers
- (none) — MG0 + MG1 done & Codex-approved; next is MG2.
